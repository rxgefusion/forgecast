# Gradle
.gradle/
build/
out/

# Loom
.fabric/
run/
remappedSrc/

# IDE
.idea/
*.iml
# .vscode/ - Loom generates launch configs here, keep them
!.vscode/launch.json
.settings/
.classpath
.project

# Secrets - a Hypixel API key is a UUID and is easy to paste somewhere and forget.
# See SecretsGuardTest for where a key SHOULD live (never in this repo).
.env
*.pem
*.p12
*.keystore
*api-key*
*apikey*
*secret*
*credentials*
*.token
